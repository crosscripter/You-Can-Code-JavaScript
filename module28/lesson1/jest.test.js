// Example test with Jest

// 1 + 2 should equal 3 (at least in this universe):
test('adds 1 + 2 to equal 3', () => {

  // We can use the expect() function to 
  // assert truth in our code.
  // In this case we "expect" 1 + 2 
  // toBe exactly 3
  expect(1 + 2).toBe(3);

});
